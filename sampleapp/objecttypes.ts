let car:{type:string, model:string, year:number}={
    type:'Toyota',
    model:'Corrola',
    year:2022
}
console.log(car.model);

//car.model=2018;
//console.log(car.model);

//car.year="hi";
//console.log(car.year);
